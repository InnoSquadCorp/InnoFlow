{
  "runCount": 10,
  "gateMetric": "mean",
  "gateTolerance": 1.0,
  "gateRole": "capture integrity blocker plus non-blocking timing trend",
  "baselineFixture": "Tests/InnoFlowTests/Fixtures/EffectTimings.baseline.jsonl",
  "regenerationBuildPath": ".build-effect-baseline-refresh",
  "regenerationEnv": "INNOFLOW_WRITE_EFFECT_BASELINE",
  "notes": [
    "The isolated release gate blocks on malformed or incomplete captures and reports metric regressions as non-blocking trend output.",
    "Use scripts/report-effect-timing-trend.sh for stricter non-blocking mean and p95 reporting.",
    "scripts/compare-effect-timings.sh exits 1 for true metric regressions and 2 for malformed or incomplete captures; the trend reporter propagates those hard failures.",
    "Refresh the baseline when Swift toolchains, CI runners, or the probe workload change materially."
  ]
}
